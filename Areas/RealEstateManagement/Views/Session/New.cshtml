@model REalEstateManagement.ViewModel.Session;

<h1>Hello</h1>

@using (Html.BeginForm("Create", "Session"))
{
    @Html.LabelFor(m => m.Email)
    @Html.TextBoxFor(m => m.Email)
    @Html.ValidationMessageFor(m => m.Email)
    <br />
    <input type="submit" value="Submit" />
}