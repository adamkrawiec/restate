@using restate.RealEstateManagement.Models
@model List<EstateUnit>

<h2>Estate Units</h2>

<ul>
  @foreach(var estateUnit in Model)
  {
    <li>
      @Html.ActionLink(estateUnit.Name, "Show", "EstateUnit", new { realEstateId = estateUnit.RealEstateId, id = estateUnit.Id})</li>
  }
</ul>